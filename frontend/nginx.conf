server {
  listen 8080;
  server_tokens off;  # do not advertise the nginx version
  client_max_body_size 1m;  # the API only accepts small JSON bodies
  root /usr/share/nginx/html;
  index index.html;

  add_header X-Content-Type-Options "nosniff" always;
  add_header X-Frame-Options "DENY" always;
  add_header Referrer-Policy "no-referrer" always;
  add_header Permissions-Policy "camera=(), microphone=(), geolocation=(), payment=()" always;
  add_header Cross-Origin-Opener-Policy "same-origin" always;
  # style-src allows 'unsafe-inline' because React sets some inline styles; scripts stay 'self' only.
  add_header Content-Security-Policy "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;

  # API calls are proxied to the backend container, so the browser only ever talks to one origin.
  location /api/ {
    proxy_pass http://backend:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header X-Request-ID $request_id;
  }

  # Single-page app: unknown paths fall back to index.html so React Router can handle them.
  location / {
    try_files $uri /index.html;
  }
}
